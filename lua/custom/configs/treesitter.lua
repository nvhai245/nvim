local options = {
  ensure_installed = { "lua", "bash", "python", "go", "gomod", "gosum", "yaml", "toml", "json", "sql", "markdown", "mermaid", "dockerfile", "vim", "vimdoc", "make" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
